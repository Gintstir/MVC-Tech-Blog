<article class="post">
    <div class="title">
        <a href="/post/{{id}}">{{title}}</a>
        <p class="user">By: {{user.username}} on {{format_date created_at}}</p>
    </div>
    <div class="content">        
        <p>{{ post_content }}</p>
    </div>
</article>